{
  "name": "workspacehub",
  "version": "1.0.0",
  "description": "Personal workspace manager for Energy Hero & AI5innovation",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node tests/run.js",
    "build:three": "esbuild vendor-three-loader.js --bundle --format=iife --outfile=vendor/three.bundle.js --minify"
  },
  "devDependencies": {
    "electron": "^28.3.0",
    "esbuild": "^0.28.0"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
